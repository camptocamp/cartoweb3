;@BLURB@

### Mapfile configuration ###

cartoclientBaseUrl =  "@CARTOCLIENT_BASE_URL@"
profile = @PROFILE@

# The mapId of the mapfile to use
mapId = projectmap2

# The id of the initial map state to be loaded on startup
initialMapStateId = projectstate

### Plugins ###

# the list of plugins to load, separated by comma
; FIXME: projectLocation is not activated because of
;  http://bugzilla.maptools.org/show_bug.cgi?id=1286
;loadPlugins = projectplugin, projectLocation, projectTable, projectTableRules
loadPlugins = projectplugin, projectTable, projectTableRules


