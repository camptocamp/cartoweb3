;@BLURB@

cartoserverDirectAccess = false

cartoclientBaseUrl =  "@CARTOCLIENT_BASE_URL@"
profile = @PROFILE@

loadPlugins = auth, hello, outline, exportPdf, exportHtml, exportCsv, exportDxf, exportRtf, views, layerReorder, testMainLocation, filterIdrecenter, testMainTableRules, accounting, throttling, exportLinkIt

; views
viewOn = true
viewablePlugins = testMainLocation, layers, query, outline

; toolPicker
toolPickerOn = true
toolbarRendering = outline

; available languages
langList = en, fr

; accounting
accountingOn = true
accountingStorage = file
