;@BLURB@

cartoserverDirectAccess = true

cartoclientBaseUrl =  "@CARTOCLIENT_BASE_URL@"
profile = @PROFILE@

loadPlugins = auth, hello, outline, exportPdf, exportHtml, exportCsv, exportDxf, views, layerReorder, testMainLocation, filterIdrecenter, testMainTableRules, accounting

; views
viewOn = true
viewablePlugins = testMainLocation, layers, query, outline

; toolPicker
toolPickerOn = true
toolbarRendering = outline

; available languages
langList = en, fr

; accounting
accountingOn = true
accountingStorage = file
