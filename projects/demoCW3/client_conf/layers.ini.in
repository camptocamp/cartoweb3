
; True if the security check of layer access is activated. It might have a minor
; impact on performances, if a large number of layer is used (to be tested).
applySecurity = true

folderId = "layerTree"

switchTargetId = "switchTarget"

;switch ID (not label) in layers.ini which is related to the default switch (in case you do not want to have default being displayed in the switch list)
relatedDefaultSwitchId = "switch1"
